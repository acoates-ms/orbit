// Disable windows linked modules for now.. until we fix them for new arch

module.exports = {
    dependencies: {
      /*
    'react-native-svg': {
        platforms: {
        windows: null,
      },
    },
    */
  },
};
